using System;
using CDSharp;

public class Examples
{
    public void ExamplesFr()
    {
        //Welcome to CD#, this is C# but so, so, SO much better.
        //First off, we have yools. Yools is basically a bool, but better.
        //Here's an example:
    }
}
